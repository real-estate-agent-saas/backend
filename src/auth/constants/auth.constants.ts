export const JWT_EXPIRATION = '30d'; // Defines how long will the backend consider the token
export const JWT_EXPIRATION_MS = 1000 * 60 * 60 * 24 * 30; // Defines how long will the browser keep the token
